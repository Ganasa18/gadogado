# Tauri backend environment (src-tauri)
#
# Copy this file to: src-tauri/.env
#
# NOTE: Keep secrets out of git.

# DEV ONLY:
# Allow recreating the RAG SQLite DB when schema user_version is older.
# - When set, the old DB will be renamed to *.bak.<timestamp> and rebuilt.
# - Do NOT enable this once you rely on the DB contents.
RAG_DB_ALLOW_RECREATE=1
